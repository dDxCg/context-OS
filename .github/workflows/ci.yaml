name: CI

on:
  push:
    branches:
      - main
      - develop
    # A release tag must run through the same CI this workflow already
    # gives every branch push - cd.yaml listens for *this* workflow's
    # completion (workflow_run) rather than re-testing, so a tag push has
    # to actually reach here first or CD never fires at all.
    tags:
      - "v*.*.*"

  pull_request:

jobs:

  test:

    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      
      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Install dependencies
        run: |
          uv sync --extra dev --frozen

      - name: Ruff
        run: |
          uv run ruff check . --fix 

      - name: Run Tests
        run: |
          uv run pytest \
            tests/unit \
            tests/integration \
            --cov=src \
            --cov-report=term-missing \
            --cov-report=xml

      - name: Upload Coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.python-version }}
          path: coverage.xml